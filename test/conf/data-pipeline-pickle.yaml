defaults:
  - pipeline/data/dataset: numpy
  - pipeline/data/tasks: generate_pickle_dataset
  - pipeline/options/compression: default
  - _self_

hydra:
  run:
    dir: ../outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}